# Extra dependencies for the preview web app (webapp.py).
# Install on top of requirements.txt:  pip install -r requirements.txt -r requirements-web.txt
flask
gunicorn
# For `webapp.py --https`: Werkzeug's adhoc self-signed cert is generated with
# the cryptography library. pyopenssl strengthens TLS for requests/urllib3 too.
cryptography
pyopenssl
