services:
  rssrob:
    build: .
    container_name: rssrob
    command: serve
    ports:
      - "8080:8080"
    volumes:
      - ./configs:/app/configs
      - ./var:/app/var
    restart: unless-stopped
