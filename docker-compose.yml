services:
  rssrob:
    build: .
    container_name: rssrob
    ports:
      - "8080:8080"
      - "5000:5000"
    volumes:
      - ./configs:/app/configs
      - ./var:/app/var
    restart: unless-stopped
